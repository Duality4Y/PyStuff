import GameObjects
import pygame
from pygame.locals import *
import sys

pygame.init();
size = width,height = 800,800;
flags = DOUBLEBUF;
screen = pygame.display.set_mode((size),flags);

white = (255,255,255);
black = (0,0,0);
red = (255,0,0);
green = (0,255,0);
blue = (0,0,255);

pygame.timer.set_timer(USEREVENT+1, 20);
