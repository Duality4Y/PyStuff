import pygame
from pygame.locals import *

screen = pygame.display.set_mode((600,400));

running = 1;

while running:
    for event in pygame.event.get():
        if event.type == QUIT:
            runnig = 0;
    screen.fill((0,0,0));
    pygame.display.flip();